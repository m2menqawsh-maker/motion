import React from "react";
import { THEMES } from "@/remotion/scenes/v0";
import type { TemplateProps } from "@registry/types";

export const THEMESWrapper = ({ surface, content, ...rest }: any) => {
  const template_props = rest.template_props || {};
  return null;
};
