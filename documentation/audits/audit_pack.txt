{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "blueprint.schema.json",
  "title": "المخطط التنفيذي — Blueprint (مصدر الحقيقة الوحيد)",
  "description": "عقد المخطط التنفيذي: يصف المشاهد والأنماط والحركات والأصوات — المصدر الوحيد للحقيقة لبناء الفيديو",
  "type": "object",
  "required": ["project_id", "version", "fps", "scenes"],
  "additionalProperties": false,
  "definitions": {
    "hexColor": {
      "type": "string",
      "pattern": "^#([0-9a-fA-F]{6}|[0-9a-fA-F]{8})$",
      "description": "لون HEX — 6 أو 8 خانات"
    },
    "anchor": {
      "type": "string",
      "enum": [
        "top-left", "top-center", "top-right",
        "center-left", "center", "center-right",
        "bottom-left", "bottom-center", "bottom-right"
      ],
      "description": "نقطة الارتكاز المكانية (9 قيم)"
    },
    "fontKey": {
      "type": "string",
      "enum": [
        "Cairo", "Tajawal", "Almarai", "IBMPlexSansArabic", "NotoKufiArabic",
        "Inter", "Manrope", "Fraunces", "JetBrainsMono"
      ],
      "description": "مفتاح الخط من القائمة المعتمدة"
    },
    "styleSurface": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "text": {
          "type": "string",
          "description": "النص المعروض"
        },
        "fontSize": {
          "type": "number",
          "minimum": 8,
          "maximum": 400,
          "description": "حجم الخط (8 إلى 400)"
        },
        "fontWeight": {
          "oneOf": [
            { "type": "number" },
            { "type": "string" }
          ],
          "description": "وزن الخط — رقم أو نص (مثل bold)"
        },
        "fontFamily": {
          "$ref": "#/definitions/fontKey",
          "description": "عائلة الخط"
        },
        "letterSpacing": {
          "type": "string",
          "description": "التباعد بين الحروف (مثل 0.05em)"
        },
        "textAlign": {
          "type": "string",
          "enum": ["right", "center", "left", "start", "end"],
          "description": "محاذاة النص"
        },
        "lineHeight": {
          "type": "number",
          "minimum": 0.5,
          "maximum": 4,
          "description": "ارتفاع السطر (0.5 إلى 4)"
        },
        "color": {
          "$ref": "#/definitions/hexColor",
          "description": "لون النص"
        },
        "background": {
          "$ref": "#/definitions/hexColor",
          "description": "لون الخلفية"
        },
        "gradient": {
          "type": "object",
          "required": ["from", "to"],
          "additionalProperties": false,
          "properties": {
            "from": { "$ref": "#/definitions/hexColor" },
            "to": { "$ref": "#/definitions/hexColor" },
            "angle": {
              "type": "number",
              "description": "زاوية التدرج بالدرجات"
            }
          },
          "description": "تدرج لوني"
        },
        "opacity": {
          "type": "number",
          "minimum": 0,
          "maximum": 1,
          "description": "الشفافية (0 إلى 1)"
        },
        "position": {
          "type": "object",
          "required": ["anchor"],
          "additionalProperties": false,
          "properties": {
            "anchor": { "$ref": "#/definitions/anchor" },
            "x": { "type": "number", "description": "إزاحة أفقية" },
            "y": { "type": "number", "description": "إزاحة عمودية" }
          },
          "description": "الموقع المكاني"
        },
        "scale": {
          "type": "number",
          "minimum": 0.1,
          "maximum": 10,
          "description": "مقياس التكبير (0.1 إلى 10)"
        },
        "rotation": {
          "type": "number",
          "minimum": -360,
          "maximum": 360,
          "description": "زاوية الدوران بالدرجات"
        },
        "width": {
          "type": "number",
          "minimum": 1,
          "description": "العرض بالبكسل (≥ 1)"
        },
        "height": {
          "type": "number",
          "minimum": 1,
          "description": "الارتفاع بالبكسل (≥ 1)"
        },
        "animation": {
          "type": "string",
          "enum": [
            "none", "fade_in", "fade_in_up", "fade_out",
            "zoom_in", "zoom_in_bounce",
            "slide_up", "slide_down", "slide_right", "slide_left",
            "typewriter", "word_flip", "text_swell"
          ],
          "description": "نوع الحركة"
        },
        "speed": {
          "type": "number",
          "minimum": 0.1,
          "maximum": 5,
          "description": "سرعة الحركة (0.1 إلى 5)"
        },
        "delay": {
          "type": "integer",
          "minimum": 0,
          "description": "تأخير بدء الحركة بالإطارات (≥ 0)"
        },
        "mode": {
          "type": "string",
          "enum": ["light", "dark"],
          "description": "وضع العرض"
        },
        "logoSrc": {
          "type": "string",
          "description": "مسار الشعار"
        },
        "brandName": {
          "type": "string",
          "description": "اسم العلامة التجارية"
        },
        "styleOverride": {
          "type": "object",
          "propertyNames": {
            "enum": [
              "borderRadius", "boxShadow", "textShadow", "filter",
              "backdropFilter", "border", "borderColor", "borderWidth",
              "textStroke", "textStrokeWidth", "padding", "gap"
            ]
          },
          "additionalProperties": {
            "oneOf": [
              { "type": "string" },
              { "type": "number" }
            ]
          },
          "description": "تجاوزات CSS مسموحة — مفاتيح من القائمة البيضاء فقط"
        }
      },
      "description": "سطح الأنماط البصرية — يجمع كل خصائص العرض في كائن واحد"
    },
    "scene": {
      "type": "object",
      "required": ["scene_id", "template", "startFrame", "durationFrames"],
      "additionalProperties": false,
      "properties": {
        "scene_id": {
          "type": "string",
          "pattern": "^scene_[0-9]+$",
          "description": "معرّف المشهد — يبدأ بـ scene_ ويتبعه رقم"
        },
        "template": {
          "type": "string",
          "pattern": "^[a-z0-9-]+$",
          "description": "اسم القالب — حروف صغيرة وأرقام وشرطات فقط"
        },
        "startFrame": {
          "type": "integer",
          "minimum": 0,
          "description": "إطار البداية (≥ 0)"
        },
        "durationFrames": {
          "type": "integer",
          "minimum": 1,
          "description": "مدة المشهد بالإطارات (≥ 1)"
        },
        "props": {
          "$ref": "#/definitions/styleSurface",
          "description": "خصائص العرض البصري للمشهد"
        },
        "media_refs": {
          "type": "array",
          "items": { "type": "string" },
          "description": "مراجع الأصول المرئية"
        },
        "sfx_ref": {
          "type": ["string", "null"],
          "description": "مرجع المؤثر الصوتي"
        },
        "captions_ref": {
          "type": ["string", "null"],
          "description": "مرجع ملف الترجمة/الكابشن"
        }
      },
      "description": "تعريف مشهد واحد"
    }
  },
  "properties": {
    "project_id": {
      "type": "string",
      "pattern": "^prj_[a-z0-9_-]+$",
      "description": "معرّف المشروع"
    },
    "version": {
      "type": "integer",
      "minimum": 1,
      "description": "رقم إصدار المخطط"
    },
    "fps": {
      "type": "integer",
      "enum": [24, 25, 30, 60],
      "description": "عدد الإطارات في الثانية"
    },
    "totalDurationFrames": {
      "type": "integer",
      "minimum": 1,
      "description": "المدة الإجمالية بالإطارات (اختياري)"
    },
    "scenes": {
      "type": "array",
      "items": { "$ref": "#/definitions/scene" },
      "minItems": 1,
      "description": "قائمة المشاهد — مشهد واحد على الأقل"
    },
    "audio": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "voiceover_ref": {
          "type": ["string", "null"],
          "description": "مرجع ملف التعليق الصوتي"
        },
        "music_ref": {
          "type": ["string", "null"],
          "description": "مرجع ملف الموسيقى"
        },
        "music_ducking_db": {
          "type": "number",
          "maximum": 0,
          "description": "مقدار خفض الموسيقى بالديسيبل (≤ 0)"
        }
      },
      "description": "إعدادات الصوت"
    }
  }
}
{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "brand.schema.json",
  "title": "الهوية البصرية — Brand Identity",
  "description": "عقد الهوية البصرية: الألوان، الخطوط، الشعار، ونبرة التصميم",
  "type": "object",
  "required": ["brandName", "colors", "fonts"],
  "additionalProperties": false,
  "definitions": {
    "hexColor": {
      "type": "string",
      "pattern": "^#([0-9a-fA-F]{6}|[0-9a-fA-F]{8})$",
      "description": "لون بصيغة HEX (6 أو 8 خانات)"
    },
    "fontKey": {
      "type": "string",
      "enum": [
        "Cairo", "Tajawal", "Almarai", "IBMPlexSansArabic", "NotoKufiArabic",
        "Inter", "Manrope", "Fraunces", "JetBrainsMono"
      ],
      "description": "مفتاح الخط من القائمة المعتمدة"
    }
  },
  "properties": {
    "brandName": {
      "type": "string",
      "minLength": 1,
      "description": "اسم العلامة التجارية"
    },
    "logoSrc": {
      "type": ["string", "null"],
      "pattern": "^assets/ready/.+\\.(svg|png|webp)$",
      "description": "مسار ملف الشعار داخل assets/ready/"
    },
    "colors": {
      "type": "object",
      "required": ["primary", "accent", "background", "text"],
      "additionalProperties": false,
      "properties": {
        "primary": { "$ref": "#/definitions/hexColor" },
        "accent": { "$ref": "#/definitions/hexColor" },
        "background": { "$ref": "#/definitions/hexColor" },
        "text": { "$ref": "#/definitions/hexColor" },
        "surface": { "$ref": "#/definitions/hexColor" }
      },
      "description": "لوحة الألوان الأساسية"
    },
    "fonts": {
      "type": "object",
      "required": ["display", "body"],
      "additionalProperties": false,
      "properties": {
        "display": { "$ref": "#/definitions/fontKey" },
        "body": { "$ref": "#/definitions/fontKey" }
      },
      "description": "خطوط العرض والمتن"
    },
    "tone": {
      "type": "string",
      "description": "نبرة التصميم البصري (اختياري)"
    }
  }
}
{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "manifest.schema.json",
  "title": "سجل الأصول — Asset Manifest",
  "description": "عقد سجل الأصول: يوثّق كل ملف وسائط في المشروع مع حالته ومصدره",
  "type": "object",
  "required": ["project_id", "generated_at", "assets"],
  "additionalProperties": false,
  "properties": {
    "project_id": {
      "type": "string",
      "pattern": "^prj_[a-z0-9_-]+$",
      "description": "معرّف المشروع"
    },
    "generated_at": {
      "type": "string",
      "format": "date-time",
      "description": "تاريخ ووقت توليد السجل"
    },
    "assets": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["asset_id", "type", "path", "source", "approved"],
        "additionalProperties": false,
        "properties": {
          "asset_id": {
            "type": "string",
            "pattern": "^ast_[a-z0-9_-]+$",
            "description": "معرّف الأصل الفريد"
          },
          "type": {
            "type": "string",
            "enum": ["image", "video", "audio", "sfx", "music", "logo"],
            "description": "نوع الأصل"
          },
          "path": {
            "type": "string",
            "pattern": "^assets/(incoming|cache|ready)/.+",
            "description": "مسار الملف داخل دورة حياة الأصول"
          },
          "source": {
            "type": "string",
            "enum": ["incoming", "cache", "ready"],
            "description": "مرحلة الأصل الحالية"
          },
          "approved": {
            "type": "boolean",
            "description": "هل تمت الموافقة على الأصل؟"
          },
          "thumb": {
            "type": ["string", "null"],
            "description": "مسار الصورة المصغرة (اختياري)"
          },
          "durationFrames": {
            "type": ["integer", "null"],
            "minimum": 0,
            "description": "مدة الأصل بالإطارات (اختياري)"
          },
          "width": {
            "type": ["integer", "null"],
            "description": "العرض بالبكسل (اختياري)"
          },
          "height": {
            "type": ["integer", "null"],
            "description": "الارتفاع بالبكسل (اختياري)"
          },
          "origin": {
            "type": "string",
            "enum": ["user", "pixabay", "pexels", "icons", "generated"],
            "description": "مصدر الأصل الأصلي"
          }
        },
        "description": "عنصر أصل واحد"
      },
      "description": "قائمة الأصول"
    }
  }
}
{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "overrides.schema.json",
  "title": "تجاوزات المشاهد — Scene Overrides",
  "description": "عقد التجاوزات: يسمح بتعديل خصائص مشاهد محددة دون تغيير المخطط الأصلي",
  "type": "object",
  "required": ["project_id"],
  "additionalProperties": false,
  "properties": {
    "project_id": {
      "type": "string",
      "pattern": "^prj_[a-z0-9_-]+$",
      "description": "معرّف المشروع"
    },
    "scenes": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["scene_id"],
        "additionalProperties": false,
        "properties": {
          "scene_id": {
            "type": "string",
            "pattern": "^scene_[0-9]+$",
            "description": "معرّف المشهد المراد تجاوزه"
          },
          "props": {
            "$ref": "blueprint.schema.json#/definitions/styleSurface",
            "description": "خصائص بصرية بديلة للمشهد"
          },
          "timing": {
            "type": "object",
            "additionalProperties": false,
            "properties": {
              "startFrame": {
                "type": "integer",
                "minimum": 0,
                "description": "إطار البداية الجديد"
              },
              "durationFrames": {
                "type": "integer",
                "minimum": 1,
                "description": "مدة المشهد الجديدة بالإطارات"
              }
            },
            "description": "توقيتات بديلة"
          }
        },
        "description": "تجاوز لمشهد واحد"
      },
      "description": "قائمة تجاوزات المشاهد"
    }
  }
}
{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "project.schema.json",
  "title": "مشروع فيديو — Project Configuration",
  "description": "عقد بيانات المشروع: يحدد الهوية، الأبعاد، اللغة، وإعدادات التعليق الصوتي",
  "type": "object",
  "required": ["project_id", "name", "aspect", "fps", "language", "voiceover", "created_at"],
  "additionalProperties": false,
  "properties": {
    "project_id": {
      "type": "string",
      "pattern": "^prj_[a-z0-9_-]+$",
      "description": "معرّف المشروع الفريد — يبدأ بـ prj_ ويتبعه حروف صغيرة وأرقام وشرطات"
    },
    "name": {
      "type": "string",
      "minLength": 1,
      "maxLength": 80,
      "description": "اسم المشروع المقروء — بين 1 و 80 حرفاً"
    },
    "aspect": {
      "type": "string",
      "enum": ["9:16", "16:9", "1:1"],
      "description": "نسبة العرض إلى الارتفاع"
    },
    "resolution": {
      "type": "object",
      "additionalProperties": false,
      "required": ["w", "h"],
      "properties": {
        "w": {
          "type": "integer",
          "enum": [1080, 1920],
          "description": "العرض بالبكسل"
        },
        "h": {
          "type": "integer",
          "enum": [1080, 1920],
          "description": "الارتفاع بالبكسل"
        }
      },
      "description": "دقة الفيديو (اختياري)"
    },
    "fps": {
      "type": "integer",
      "enum": [24, 25, 30, 60],
      "description": "عدد الإطارات في الثانية"
    },
    "language": {
      "type": "string",
      "enum": ["ar", "en"],
      "description": "لغة المشروع الأساسية"
    },
    "brand": {
      "type": ["string", "null"],
      "description": "معرّف الهوية البصرية المرتبطة (أو null)"
    },
    "created_at": {
      "type": "string",
      "format": "date-time",
      "description": "تاريخ ووقت إنشاء المشروع بصيغة ISO 8601"
    },
    "voiceover": {
      "type": "object",
      "required": ["mode"],
      "additionalProperties": false,
      "properties": {
        "mode": {
          "type": "string",
          "enum": ["upload", "tts", "none"],
          "description": "وضع التعليق الصوتي: رفع ملف، توليد بالذكاء الاصطناعي، أو بدون"
        },
        "file": {
          "type": ["string", "null"],
          "description": "مسار ملف التعليق الصوتي المرفوع"
        },
        "voice_id": {
          "type": ["string", "null"],
          "description": "معرّف الصوت لمحرك TTS"
        }
      },
      "allOf": [
        {
          "if": {
            "properties": { "mode": { "const": "upload" } },
            "required": ["mode"]
          },
          "then": {
            "required": ["mode", "file"],
            "properties": {
              "file": {
                "type": "string",
                "minLength": 1
              }
            }
          }
        },
        {
          "if": {
            "properties": { "mode": { "const": "tts" } },
            "required": ["mode"]
          },
          "then": {
            "required": ["mode", "voice_id"],
            "properties": {
              "voice_id": {
                "type": "string",
                "minLength": 1
              }
            }
          }
        }
      ]
    }
  }
}
{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "state.schema.json",
  "title": "حالة المشروع — Project State",
  "description": "عقد حالة المشروع: يتتبع المراحل والبوابات والحالة الحالية لخط الإنتاج",
  "type": "object",
  "required": ["project_id", "current_stage", "stages", "gates", "updated_at"],
  "additionalProperties": false,
  "definitions": {
    "stageEntry": {
      "type": "object",
      "required": ["status"],
      "additionalProperties": false,
      "properties": {
        "status": {
          "type": "string",
          "enum": ["pending", "running", "done", "failed"],
          "description": "حالة المرحلة"
        },
        "started_at": {
          "type": ["string", "null"],
          "format": "date-time",
          "description": "وقت البدء"
        },
        "finished_at": {
          "type": ["string", "null"],
          "format": "date-time",
          "description": "وقت الانتهاء"
        }
      },
      "description": "سجل مرحلة واحدة"
    },
    "gateEntry": {
      "type": "object",
      "required": ["status"],
      "additionalProperties": false,
      "properties": {
        "status": {
          "type": "string",
          "enum": ["locked", "awaiting", "approved", "rejected"],
          "description": "حالة البوابة"
        },
        "approved_by": {
          "type": ["string", "null"],
          "description": "من وافق على البوابة"
        },
        "approved_at": {
          "type": ["string", "null"],
          "format": "date-time",
          "description": "وقت الموافقة"
        },
        "note": {
          "type": ["string", "null"],
          "description": "ملاحظة إضافية"
        }
      },
      "description": "سجل بوابة واحدة"
    }
  },
  "properties": {
    "project_id": {
      "type": "string",
      "pattern": "^prj_[a-z0-9_-]+$",
      "description": "معرّف المشروع"
    },
    "current_stage": {
      "type": "integer",
      "minimum": 0,
      "maximum": 3,
      "description": "المرحلة الحالية (0 إلى 3)"
    },
    "last_error": {
      "type": ["string", "null"],
      "description": "آخر خطأ مسجل (أو null)"
    },
    "stages": {
      "type": "object",
      "required": ["0", "1", "2", "3"],
      "additionalProperties": false,
      "properties": {
        "0": { "$ref": "#/definitions/stageEntry" },
        "1": { "$ref": "#/definitions/stageEntry" },
        "2": { "$ref": "#/definitions/stageEntry" },
        "3": { "$ref": "#/definitions/stageEntry" }
      },
      "description": "سجل المراحل الأربعة"
    },
    "gates": {
      "type": "object",
      "required": ["gate_1", "gate_2", "gate_3"],
      "additionalProperties": false,
      "properties": {
        "gate_1": { "$ref": "#/definitions/gateEntry" },
        "gate_2": { "$ref": "#/definitions/gateEntry" },
        "gate_3": { "$ref": "#/definitions/gateEntry" }
      },
      "description": "سجل بوابات الجودة الثلاث"
    },
    "updated_at": {
      "type": "string",
      "format": "date-time",
      "description": "تاريخ ووقت آخر تحديث"
    }
  }
}
#!/usr/bin/env python3
"""
فاحص العقود — Schema Validator
يفحص ملفات JSON لمشروع فيديو مقابل العقود المعرّفة في schemas/

الاستخدام:
    python scripts/validate_schemas.py <project_dir>

المخرج:
    ✅ أو ❌ لكل ملف مع تفاصيل الأخطاء
    Exit 0 = كل الملفات الموجودة سليمة
    Exit 1 = خطأ واحد على الأقل
"""

import json
import sys
import os
from pathlib import Path

try:
    import jsonschema
    from jsonschema import Draft7Validator, RefResolver, ValidationError
except ImportError:
    print("❌ مكتبة jsonschema غير مثبتة. شغّل: pip install jsonschema")
    sys.exit(1)


# خريطة الملفات ← العقود
FILE_SCHEMA_MAP = {
    "project.json": "project.schema.json",
    "blueprint.json": "blueprint.schema.json",
    "brand.json": "brand.schema.json",
    "overrides.json": "overrides.schema.json",
    "manifest.json": "manifest.schema.json",
    "state.json": "state.schema.json",
}


def load_json(filepath: Path) -> dict:
    """يقرأ ملف JSON ويعيد المحتوى كقاموس."""
    with open(filepath, "r", encoding="utf-8") as f:
        return json.load(f)


def build_resolver(schemas_dir: Path) -> RefResolver:
    """
    يبني RefResolver لحل مراجع $ref العابرة للملفات.
    يستخدم base_uri = مجلد schemas/ كـ URI أساسي.
    """
    schema_store = {}
    for schema_file in schemas_dir.glob("*.schema.json"):
        schema = load_json(schema_file)
        schema_id = schema.get("$id", schema_file.name)
        schema_store[schema_id] = schema

    base_uri = schemas_dir.as_uri() + "/"
    # نستخدم مخطط فارغ كمرجع أساسي، والـ store يحتوي كل العقود
    resolver = RefResolver(base_uri, {}, store=schema_store)
    return resolver


def validate_file(
    filepath: Path,
    schema: dict,
    resolver: RefResolver,
) -> list[str]:
    """
    يفحص ملف JSON مقابل عقد معيّن.
    يعيد قائمة فارغة عند النجاح، أو قائمة رسائل خطأ.
    """
    try:
        data = load_json(filepath)
    except json.JSONDecodeError as e:
        return [f"خطأ في قراءة JSON: {e}"]

    validator = Draft7Validator(schema, resolver=resolver)
    errors = []
    for error in sorted(validator.iter_errors(data), key=lambda e: list(e.path)):
        json_path = " → ".join(str(p) for p in error.absolute_path) or "(الجذر)"
        errors.append(f"  [{json_path}] {error.message}")
    return errors


def main():
    if len(sys.argv) != 2:
        print("الاستخدام: python scripts/validate_schemas.py <project_dir>")
        sys.exit(1)

    project_dir = Path(sys.argv[1])
    if not project_dir.is_dir():
        print(f"❌ المجلد غير موجود: {project_dir}")
        sys.exit(1)

    # حدد مجلد العقود (schemas/) — بجانب scripts/ أو في الجذر
    script_dir = Path(__file__).resolve().parent
    workspace_root = script_dir.parent
    schemas_dir = workspace_root / "schemas"

    if not schemas_dir.is_dir():
        print(f"❌ مجلد العقود غير موجود: {schemas_dir}")
        sys.exit(1)

    resolver = build_resolver(schemas_dir)
    has_errors = False
    checked = 0
    skipped = 0

    for data_filename, schema_filename in FILE_SCHEMA_MAP.items():
        data_path = project_dir / data_filename
        schema_path = schemas_dir / schema_filename

        if not data_path.exists():
            print(f"⬜ {data_filename:<20s} — غير موجود (تخطّي)")
            skipped += 1
            continue

        schema = load_json(schema_path)
        errors = validate_file(data_path, schema, resolver)
        checked += 1

        if errors:
            has_errors = True
            print(f"❌ {data_filename:<20s} — {len(errors)} خطأ:")
            for err in errors:
                print(err)
        else:
            print(f"✅ {data_filename:<20s} — سليم")

    print(f"\n{'─' * 50}")
    print(f"النتيجة: {checked} فُحص | {skipped} تُخطّي | {'❌ فشل' if has_errors else '✅ نجاح'}")
    sys.exit(1 if has_errors else 0)


if __name__ == "__main__":
    main()
#!/usr/bin/env python3
"""
اختبارات العقود — Schema Tests
يفحص 6 حالات اختبار (fixtures داخل الذاكرة) لضمان صحة العقود وصرامتها.

الاستخدام:
    python scripts/test_schemas.py

المخرج:
    PASS/FAIL لكل حالة + ملخص نهائي
    Exit 0 = 6/6 PASS
    Exit 1 = أي FAIL
"""

import json
import sys
import copy
from pathlib import Path

try:
    import jsonschema
    from jsonschema import Draft7Validator, RefResolver
except ImportError:
    print("❌ مكتبة jsonschema غير مثبتة. شغّل: pip install jsonschema")
    sys.exit(1)


def load_json(filepath: Path) -> dict:
    with open(filepath, "r", encoding="utf-8") as f:
        return json.load(f)


def build_resolver(schemas_dir: Path) -> RefResolver:
    schema_store = {}
    for schema_file in schemas_dir.glob("*.schema.json"):
        schema = load_json(schema_file)
        schema_id = schema.get("$id", schema_file.name)
        schema_store[schema_id] = schema

    base_uri = schemas_dir.as_uri() + "/"
    resolver = RefResolver(base_uri, {}, store=schema_store)
    return resolver


def validate(schema: dict, data: dict, resolver: RefResolver) -> list[str]:
    validator = Draft7Validator(schema, resolver=resolver)
    return [e.message for e in validator.iter_errors(data)]


# ──────────────────────────────────────────────
# بيانات العينات السليمة (تُنسخ وتُعدَّل لحالات الفشل)
# ──────────────────────────────────────────────

VALID_PROJECT = {
    "project_id": "prj_test-01",
    "name": "مشروع اختباري",
    "aspect": "9:16",
    "fps": 30,
    "language": "ar",
    "brand": None,
    "created_at": "2026-09-06T12:00:00+03:00",
    "voiceover": {
        "mode": "upload",
        "file": "assets/incoming/vo.wav",
        "voice_id": None,
    },
}

VALID_BLUEPRINT = {
    "project_id": "prj_test-01",
    "version": 1,
    "fps": 30,
    "scenes": [
        {
            "scene_id": "scene_01",
            "template": "text-reveal",
            "startFrame": 0,
            "durationFrames": 90,
            "props": {
                "text": "اختبار",
                "fontSize": 48,
                "fontFamily": "Cairo",
                "color": "#00F5FF",
                "animation": "fade_in",
                "speed": 1,
                "delay": 0,
                "position": {"anchor": "center"},
                "styleOverride": {
                    "borderRadius": "8px",
                },
            },
        }
    ],
}

VALID_BRAND = {
    "brandName": "Test Brand",
    "logoSrc": "assets/ready/logo.svg",
    "colors": {
        "primary": "#00F5FF",
        "accent": "#FFD700",
        "background": "#0A0E27",
        "text": "#FFFFFF",
    },
    "fonts": {"display": "Cairo", "body": "Inter"},
}

VALID_OVERRIDES = {
    "project_id": "prj_test-01",
    "scenes": [
        {
            "scene_id": "scene_01",
            "props": {"fontSize": 64, "color": "#FF0000"},
            "timing": {"startFrame": 10, "durationFrames": 80},
        }
    ],
}

VALID_MANIFEST = {
    "project_id": "prj_test-01",
    "generated_at": "2026-09-06T12:00:00+03:00",
    "assets": [
        {
            "asset_id": "ast_img-01",
            "type": "image",
            "path": "assets/ready/bg.png",
            "source": "ready",
            "approved": True,
        }
    ],
}

VALID_STATE = {
    "project_id": "prj_test-01",
    "current_stage": 0,
    "last_error": None,
    "stages": {
        "0": {"status": "running", "started_at": "2026-09-06T12:00:00Z", "finished_at": None},
        "1": {"status": "pending", "started_at": None, "finished_at": None},
        "2": {"status": "pending", "started_at": None, "finished_at": None},
        "3": {"status": "pending", "started_at": None, "finished_at": None},
    },
    "gates": {
        "gate_1": {"status": "locked", "approved_by": None, "approved_at": None, "note": None},
        "gate_2": {"status": "locked", "approved_by": None, "approved_at": None, "note": None},
        "gate_3": {"status": "locked", "approved_by": None, "approved_at": None, "note": None},
    },
    "updated_at": "2026-09-06T12:00:00Z",
}


def main():
    script_dir = Path(__file__).resolve().parent
    workspace_root = script_dir.parent
    schemas_dir = workspace_root / "schemas"

    if not schemas_dir.is_dir():
        print(f"❌ مجلد العقود غير موجود: {schemas_dir}")
        sys.exit(1)

    resolver = build_resolver(schemas_dir)

    project_schema = load_json(schemas_dir / "project.schema.json")
    blueprint_schema = load_json(schemas_dir / "blueprint.schema.json")
    brand_schema = load_json(schemas_dir / "brand.schema.json")
    overrides_schema = load_json(schemas_dir / "overrides.schema.json")
    manifest_schema = load_json(schemas_dir / "manifest.schema.json")
    state_schema = load_json(schemas_dir / "state.schema.json")

    results = []

    # ──────────────────────────────────────────────
    # اختبار 1: عينة سليمة كاملة تمر
    # ──────────────────────────────────────────────
    test_name = "1. عينة سليمة كاملة تمرّ بنجاح"
    all_valid = True
    for label, schema, data in [
        ("project", project_schema, VALID_PROJECT),
        ("blueprint", blueprint_schema, VALID_BLUEPRINT),
        ("brand", brand_schema, VALID_BRAND),
        ("overrides", overrides_schema, VALID_OVERRIDES),
        ("manifest", manifest_schema, VALID_MANIFEST),
        ("state", state_schema, VALID_STATE),
    ]:
        errs = validate(schema, data, resolver)
        if errs:
            all_valid = False
            print(f"    ✗ {label}: {errs}")
    results.append((test_name, all_valid))

    # ──────────────────────────────────────────────
    # اختبار 2: لون "#ZZZ999" يُرفض
    # ──────────────────────────────────────────────
    test_name = '2. لون "#ZZZ999" يُرفض'
    bad_color = copy.deepcopy(VALID_BLUEPRINT)
    bad_color["scenes"][0]["props"]["color"] = "#ZZZ999"
    errs = validate(blueprint_schema, bad_color, resolver)
    results.append((test_name, len(errs) > 0))

    # ──────────────────────────────────────────────
    # اختبار 3: styleOverride بمفتاح zIndex يُرفض
    # ──────────────────────────────────────────────
    test_name = "3. styleOverride بمفتاح zIndex يُرفض"
    bad_override = copy.deepcopy(VALID_BLUEPRINT)
    bad_override["scenes"][0]["props"]["styleOverride"]["zIndex"] = 10
    errs = validate(blueprint_schema, bad_override, resolver)
    results.append((test_name, len(errs) > 0))

    # ──────────────────────────────────────────────
    # اختبار 4: voiceover mode=upload بدون file يُرفض
    # ──────────────────────────────────────────────
    test_name = "4. voiceover mode=upload بدون file يُرفض"
    bad_vo = copy.deepcopy(VALID_PROJECT)
    bad_vo["voiceover"] = {"mode": "upload", "file": None, "voice_id": None}
    errs = validate(project_schema, bad_vo, resolver)
    results.append((test_name, len(errs) > 0))

    # ──────────────────────────────────────────────
    # اختبار 5: template بقيمة "Text_Reveal!" يُرفض
    # ──────────────────────────────────────────────
    test_name = '5. template بقيمة "Text_Reveal!" يُرفض'
    bad_template = copy.deepcopy(VALID_BLUEPRINT)
    bad_template["scenes"][0]["template"] = "Text_Reveal!"
    errs = validate(blueprint_schema, bad_template, resolver)
    results.append((test_name, len(errs) > 0))

    # ──────────────────────────────────────────────
    # اختبار 6: gate بحالة "maybe" يُرفض
    # ──────────────────────────────────────────────
    test_name = '6. gate بحالة "maybe" يُرفض'
    bad_gate = copy.deepcopy(VALID_STATE)
    bad_gate["gates"]["gate_1"]["status"] = "maybe"
    errs = validate(state_schema, bad_gate, resolver)
    results.append((test_name, len(errs) > 0))

    # ──────────────────────────────────────────────
    # ملخص النتائج
    # ──────────────────────────────────────────────
    print("=" * 55)
    print("  اختبارات العقود — Schema Contract Tests")
    print("=" * 55)
    passed = 0
    for name, ok in results:
        status = "PASS ✅" if ok else "FAIL ❌"
        print(f"  {status}  {name}")
        if ok:
            passed += 1
    total = len(results)
    print("─" * 55)
    print(f"  النتيجة: {passed}/{total} {'— نجاح كامل ✅' if passed == total else '— يوجد فشل ❌'}")
    print("=" * 55)
    sys.exit(0 if passed == total else 1)


if __name__ == "__main__":
    main()
{
  "project_id": "prj_demo",
  "version": 1,
  "fps": 30,
  "totalDurationFrames": 450,
  "scenes": [
    {
      "scene_id": "scene_01",
      "template": "text-reveal",
      "startFrame": 0,
      "durationFrames": 150,
      "props": {
        "text": "مرحباً بالعالم",
        "fontSize": 64,
        "fontWeight": 700,
        "fontFamily": "Cairo",
        "textAlign": "center",
        "lineHeight": 1.4,
        "color": "#00F5FF",
        "background": "#0A0E27",
        "opacity": 1,
        "position": { "anchor": "center", "x": 0, "y": -100 },
        "animation": "fade_in_up",
        "speed": 1.2,
        "delay": 5,
        "mode": "dark",
        "styleOverride": {
          "borderRadius": "16px",
          "textShadow": "0 0 20px rgba(0,245,255,0.5)"
        }
      },
      "media_refs": ["ast_bg-01"],
      "sfx_ref": "ast_whoosh-01",
      "captions_ref": null
    },
    {
      "scene_id": "scene_02",
      "template": "split-screen",
      "startFrame": 150,
      "durationFrames": 180,
      "props": {
        "text": "المحتوى الثاني",
        "fontSize": 48,
        "fontFamily": "IBMPlexSansArabic",
        "color": "#FFD700",
        "animation": "slide_right",
        "speed": 1,
        "delay": 0,
        "position": { "anchor": "center-right" }
      },
      "media_refs": [],
      "sfx_ref": null,
      "captions_ref": null
    },
    {
      "scene_id": "scene_03",
      "template": "outro-badge",
      "startFrame": 330,
      "durationFrames": 120,
      "props": {
        "text": "تابعنا",
        "fontSize": 56,
        "fontFamily": "Almarai",
        "color": "#FFFFFF",
        "background": "#1A1A2E",
        "gradient": { "from": "#0A0E27", "to": "#1A1A2E", "angle": 135 },
        "animation": "zoom_in_bounce",
        "speed": 0.8,
        "delay": 10,
        "logoSrc": "assets/ready/logo.svg",
        "brandName": "Demo Brand",
        "position": { "anchor": "bottom-center", "x": 0, "y": -60 }
      },
      "media_refs": ["ast_logo-01"],
      "sfx_ref": "ast_boom-01",
      "captions_ref": null
    }
  ],
  "audio": {
    "voiceover_ref": "ast_vo-01",
    "music_ref": "ast_music-01",
    "music_ducking_db": -12
  }
}
{
  "brandName": "Demo Brand",
  "logoSrc": "assets/ready/logo.svg",
  "colors": {
    "primary": "#00F5FF",
    "accent": "#FFD700",
    "background": "#0A0E27",
    "text": "#FFFFFF",
    "surface": "#1A1A2E"
  },
  "fonts": {
    "display": "Cairo",
    "body": "IBMPlexSansArabic"
  },
  "tone": "تقني عصري"
}
{
  "project_id": "prj_demo",
  "generated_at": "2026-09-06T12:00:00+03:00",
  "assets": [
    {
      "asset_id": "ast_bg-01",
      "type": "image",
      "path": "assets/ready/bg_cyber.png",
      "source": "ready",
      "approved": true,
      "thumb": null,
      "durationFrames": null,
      "width": 1080,
      "height": 1920,
      "origin": "pixabay"
    },
    {
      "asset_id": "ast_vo-01",
      "type": "audio",
      "path": "assets/ready/vo_demo_normalized.wav",
      "source": "ready",
      "approved": true,
      "thumb": null,
      "durationFrames": 450,
      "width": null,
      "height": null,
      "origin": "user"
    },
    {
      "asset_id": "ast_whoosh-01",
      "type": "sfx",
      "path": "assets/cache/whoosh_metal.mp3",
      "source": "cache",
      "approved": true,
      "thumb": null,
      "durationFrames": 15,
      "width": null,
      "height": null,
      "origin": "pexels"
    },
    {
      "asset_id": "ast_music-01",
      "type": "music",
      "path": "assets/ready/bgm_cinematic.mp3",
      "source": "ready",
      "approved": true,
      "thumb": null,
      "durationFrames": 900,
      "width": null,
      "height": null,
      "origin": "pixabay"
    },
    {
      "asset_id": "ast_logo-01",
      "type": "logo",
      "path": "assets/ready/logo.svg",
      "source": "ready",
      "approved": true,
      "thumb": null,
      "durationFrames": null,
      "width": 200,
      "height": 200,
      "origin": "user"
    },
    {
      "asset_id": "ast_boom-01",
      "type": "sfx",
      "path": "assets/incoming/boom_cinematic.wav",
      "source": "incoming",
      "approved": false,
      "thumb": null,
      "durationFrames": 30,
      "width": null,
      "height": null,
      "origin": "generated"
    }
  ]
}
{
  "project_id": "prj_demo",
  "scenes": [
    {
      "scene_id": "scene_02",
      "props": {
        "fontSize": 52,
        "color": "#FF6B6B"
      },
      "timing": {
        "startFrame": 160,
        "durationFrames": 170
      }
    }
  ]
}
{
  "project_id": "prj_demo",
  "name": "مشروع تجريبي لفحص العقود",
  "aspect": "9:16",
  "resolution": { "w": 1080, "h": 1920 },
  "fps": 30,
  "language": "ar",
  "brand": "demo-brand",
  "created_at": "2026-09-06T12:00:00+03:00",
  "voiceover": {
    "mode": "upload",
    "file": "assets/incoming/vo_demo.wav",
    "voice_id": null
  }
}
{
  "project_id": "prj_demo",
  "current_stage": 1,
  "last_error": null,
  "stages": {
    "0": {
      "status": "done",
      "started_at": "2026-09-06T11:50:00+03:00",
      "finished_at": "2026-09-06T11:55:00+03:00"
    },
    "1": {
      "status": "running",
      "started_at": "2026-09-06T11:55:00+03:00",
      "finished_at": null
    },
    "2": {
      "status": "pending",
      "started_at": null,
      "finished_at": null
    },
    "3": {
      "status": "pending",
      "started_at": null,
      "finished_at": null
    }
  },
  "gates": {
    "gate_1": {
      "status": "awaiting",
      "approved_by": null,
      "approved_at": null,
      "note": "في انتظار مراجعة حزمة الميديا"
    },
    "gate_2": {
      "status": "locked",
      "approved_by": null,
      "approved_at": null,
      "note": null
    },
    "gate_3": {
      "status": "locked",
      "approved_by": null,
      "approved_at": null,
      "note": null
    }
  },
  "updated_at": "2026-09-06T12:00:00+03:00"
}
jsonschema>=4.0.0
# 🤝 دليل المساهمة والحوكمة (Contributing & Development Guide)

مرحباً بك في دليل التطوير والمساهمة في مشروع **Clean Video Workspace**.
هذا المستند يحدد القواعد والمعايير الصارمة لإدارة خط الإنتاج، سواء للمطورين البشريين أو للوكلاء الأذكياء (Autonomous AI Agents).

---

## 🎯 المبادئ الجوهرية (Core Principles)

1. **الاعتماد على البيئة المحلية (Local-First):**
   كل العمليات الحسابية والمعالجات الثقيلة (تطبيع الصوت، معالجة الفيديو، ضغط الأصول) تتم محلياً باستخدام سكريبتات Python و FFmpeg وأدوات Node.js المعتمدة.
2. **صفر ارتجال (Zero-Hallucination Policy):**
   ممنوع كتابة كود حركة من الصفر أو تخمين توقيتات. كل حركة تعتمد على قوالب معتمدة من `@templates` و `@engine`، وكل توقيت مستخرج من أداة تحليل فعلية (`04_timings.json`).
3. **احترام البوابات والأقفال الميكانيكية (Strict Hard Stops):**
   لا يسمح بتجاوز أي مرحلة من مراحل الإنتاج دون استيفاء الشروط البرمجية والموافقة الصريحة.

---

## 🛡️ بوابات الجودة الأربعة (The 4 Production Gates)

قبل دمج أي كود أو الانتقال بين المراحل، يجب اجتياز البوابات التالية:

```mermaid
flowchart LR
    G1[Gate 1: Media Gate] --> G2[Gate 2: Plan Gate]
    G2 --> G3[Gate 3: Code & Motion Gate]
    G3 --> G4[Gate 4: Studio & Render Lock]
```

### 1. Gate 1: فحص وتطبيع الميديا
- يجب تطبيع الصوت البشري (Voiceover) عند **`-16 LUFS`**.
- يجب تطبيع المؤثرات الصوتية (SFX) والموسيقى عند **`-24 LUFS`**.
- تحويل الفيديوهات إلى صيغة متوافقة (All-Intra, GOP=1, yuv420p) لمنع أخطاء MediaPlaybackError في Remotion.

### 2. Gate 2: سلامة الخطة والعمود الفقري
- ممنوع كتابة خطة مشهد بدون ملف توقيتات فعلي مستخرج عبر `analyze_voiceover` (`04_timings.json`).
- يجب ربط كل حركة ونطق كلمة بالملي ثانية (Frame-Perfect Sync).

### 3. Gate 3: سلامة قوالب الكود والحركة
- **حظر تام**: يمنع منعاً باتاً استدعاء `spring()` أو `interpolate()` مباشرة داخل ملفات `Scene*.tsx`.
- كل مشهد يجب أن يستورد قالباً معتمداً من `@templates` المدرجة في `TEMPLATE_INDEX.md`.
- فحص الكود يتم آلياً بواسطة `code_template_gate.py` و `motion_validator.py`.

### 4. Gate 4: القفل الميكانيكي والرندر النهائي
- أمر `npx remotion render` أو `npm run render` محظور في الـ Terminal ومحمي بواسطة `package.json`.
- الرندر متاح فقط عبر:
  ```bash
  python .agents/plugins/super-video-maker-plugin/scripts/render_project.py <project_id>
  ```
- لا يعمل سكريبت الرندر إلا بعد موافقة المستخدم في Remotion Studio وإنشاء ملف التوقيع اليدوي `.studio_approved`.

---

## 🎨 المعايير الفنية والجمالية (Motion & Design Standards)

* **الخطوط:** استخدام خطوط هندسية حديثة (مثل `Alexandria`، `Cairo`، `IBM Plex Sans Arabic` للعناوين، و `JetBrains Mono` للأكواد).
* **النصوص العربية (RTL):** فرض `direction: 'rtl'` و `flex-wrap` وإضافة `willChange: 'transform'` للحاويات المتحركة لمنع تشوه الأحرف (Sub-pixel rendering bug).
* **المسافات والتنفس (Breathing Room):** ترك هوامش واسعة (40px - 80px) بين العناصر والبطاقات، وتغطية كامل الشاشة (9:16) بدون فراغات ميتة.
* **الخلفيات:** تجنب القطع المفاجئ (Hard Cut) واستخدام تدفق لوني أو شبكي متصل ومستمر عبر المشاهد.

---

## 🚫 محظورات التطوير (Strict Prohibitions)

* ❌ تعديل ملفات `.agents/plugins/super-video-maker-plugin/` إلا في حال ترقية المنظومة بطلب صريح.
* ❌ تجاوز البوابات يدويًا أو إنشاء سكربتات موافقة وهمية.
* ❌ استخدام سكريبتات خارجية مثل `yt-dlp` أو `curl` بدلاً من خوادم MCP المعتمدة.
* ❌ ترك ملفات تجارب مؤقتة في جذر المشروع.

---

## 📜 عقود البيانات وفاحص العقود (Data Contracts & Schema Validator)

يستخدم المشروع عقود بيانات صارمة (JSON Schema draft-07) لضمان صحة كل ملفات المشروع قبل الدخول في خط الإنتاج.

### العقود المتاحة (في مجلد `schemas/`)

| العقد | الملف | الوصف |
| :--- | :--- | :--- |
| المشروع | `project.schema.json` | هوية المشروع، الأبعاد، اللغة، التعليق الصوتي |
| المخطط التنفيذي | `blueprint.schema.json` | المشاهد، الأنماط، الحركات — مصدر الحقيقة الوحيد |
| الهوية البصرية | `brand.schema.json` | الألوان، الخطوط، الشعار |
| التجاوزات | `overrides.schema.json` | تعديلات على مشاهد محددة (يستخدم `$ref` عابر للملفات) |
| سجل الأصول | `manifest.schema.json` | توثيق كل ملفات الوسائط ومصادرها |
| حالة المشروع | `state.schema.json` | تتبع المراحل والبوابات |

### تشغيل فاحص العقود

لفحص ملفات مشروع معيّن مقابل العقود:

```bash
# تثبيت المتطلبات (مرة واحدة)
pip install -r requirements.txt

# فحص مشروع (يفحص الملفات الموجودة فقط — الناقص يُتخطّى)
python scripts/validate_schemas.py <project_dir>

# مثال: فحص المشروع التجريبي
python scripts/validate_schemas.py schemas/examples/prj_demo
```

المخرج:
- `✅` = الملف سليم ومطابق للعقد
- `❌` = خطأ مع تفاصيل المسار والرسالة
- `⬜` = الملف غير موجود (تخطّي وليس خطأ)
- Exit code `0` = نجاح | `1` = فشل

### تشغيل اختبارات العقود

```bash
# تشغيل 6 اختبارات آلية (fixtures داخل الذاكرة)
python scripts/test_schemas.py
```

الاختبارات تغطي: عينة سليمة كاملة، رفض ألوان غير صالحة، رفض مفاتيح `styleOverride` غير مسموحة، فرض شروط `voiceover`، أنماط `template`، وحالات `gate`.

### قواعد تطوير العقود

- ❌ **ممنوع** تعديل عقد قائم بشكل يكسر التوافقية العكسية (Breaking Change).
- ✅ **مسموح** إضافة حقول اختيارية جديدة أو عقود جديدة (Additive Only).
- ✅ كل عقد يستخدم `additionalProperties: false` في كل مستوى `object`.
- ✅ كل عقد يدعم أوصاف عربية للحقول.

---
> للمزيد من التفاصيل المعمارية، تفضل بزيارة [documentation/README.md](file:///c:/video/clean-video-workspace/documentation/README.md).
