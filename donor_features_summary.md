# 📦 تفاصيل الميزات المعزولة (Donor Features)

هذا الملف يوثق جميع الميزات، المجلدات، والأنظمة التي تم عزلها من النسخة الاحتياطية ووضعها في مجلد `donor_features`. هذه الميزات تمثل المكونات اللازمة لبناء "تطبيق واجهة المستخدم (GUI)" والبيئة الموجهة بالبيانات (Data-Driven).

## 1. عقود البيانات (JSON Schemas)
- **المسار:** `donor_features/schemas/`
- **الغرض:** توفير طبقة حماية وضمان جودة (Validation) تعتمد على `JSON Schema draft-07`. لا يمر أي ملف مشروع إلى خط الإنتاج إلا إذا كان مطابقاً للعقد (مثل عقد المشروع، المخطط التنفيذي، والهوية البصرية).
- **أهم الملفات:** `project.schema.json`, `blueprint.schema.json`, `brand.schema.json`, `manifest.schema.json`.

## 2. عقود واجهات التحرير (TypeScript Contracts)
- **المسار:** `donor_features/contracts/`
- **الغرض:** مطابقة عقود `JSON` في بيئة `Node.js/React`. توفر أنواع `Types` ومدققات وقت التشغيل (Runtime Validators) لضمان أن قوالب `Remotion` تستخدم بيانات صحيحة ومطابقة فقط.
- **القاعدة الذهبية:** يمنع تعديل أي عقد هنا دون تحديث الـ JSON Schema المقابل أولاً.

## 3. سجل القوالب والمؤثرات (Template Registry & Effects)
- **المسار:** `donor_features/registry/`
- **الغرض:** يعمل كدليل (Catalog) يربط القوالب والمؤثرات المتاحة في النظام مع واجهة المستخدم ليتم اختيارها ديناميكياً.
- **أهم الملفات:** 
  - `template-registry.tsx`: السجل المرجعي لـ 27 قالباً معتمداً.
  - `effects-catalog.ts`: مفردات التأثيرات (44 تأثيراً حركياً للكاميرا والصوت).

## 4. قوالب الحركة ومغلف النظام (Templates & Wrappers)
- **المسار:** `donor_features/templates/`
- **الغرض:** يحتوي على مكونات `React/Remotion` التي تبني المشاهد فعلياً، إضافة إلى الجسر الذي يربط مؤثرات المحرك.
- **الميزة المعمارية:** جميع القوالب تستخدم الغلاف الإلزامي `TemplateWrapper` لمعالجة الألوان، الخطوط، واللغة العربية (RTL) تلقائياً، ويُمنع استخدام كود الحركة `spring()` مباشرة داخلها للحفاظ على التوافق.

## 5. بيئة البناء الحتمي الموجهة بالبيانات (Deterministic Build)
- **المسار:** `donor_features/build/`
- **الغرض:** تحويل بيئة البناء لتعمل بشكل "حتمي". بدلاً من كتابة كود `React` لكل مشروع جديد، يقوم مكون `BlueprintVideo.tsx` بقراءة ملف الـ JSON المعتمد وتجميع المشاهد والمؤثرات ديناميكياً.

## 6. طبقة الخدمة المحلية (Local API - FastAPI)
- **المسار:** `donor_features/api/`
- **الغرض:** واجهة برمجة تطبيقات (API) تغلف كافة سكربتات بايثون الخاصة بخط الإنتاج (مثل فحص الصوت، التطبيع، البوابات، والرندر).
- **الفائدة للـ GUI:** تتيح لتطبيق واجهة المستخدم التواصل مع المحرك وإرسال الأوامر عبر `HTTP` ومراقبة تقدم عملية المعالجة والرندر حياً عبر `WebSocket`.

## 7. سكربتات مساعدة وبوابات (Scripts)
- **المسار:** `donor_features/scripts/`
- **الغرض:** أدوات متفرقة تخدم المنظومة الجديدة، مثل التحقق من العقود، استخراج التوثيق، واختبارات الـ End-to-End.
- **أهم الملفات:** `validate_schemas.py`, `e2e_data_render.py`, `dev_render_blueprint.ts`.

## 8. بيئة الاختبارات (Tests)
- **المسار:** `donor_features/tests/`
- **الغرض:** ضمان استقرار كل "قطعة غيار" قبل وأثناء دمجها.
- **المحتويات:** اختبارات `Vitest` لعقود الـ TypeScript وللسجل، واختبارات `Pytest` لنقاط الـ API.

---

### 💡 خارطة الطريق المقترحة (Integration Roadmap)
لبدء تحويل المشروع الحالي إلى تطبيق متكامل (GUI)، التسلسل المعماري الأمثل لدمج هذه الميزات هو:
1. **نقل العقود (Schemas & Contracts):** لتأسيس لغة تخاطب قوية وموحدة (Data Types).
2. **نقل القوالب وسجل القوالب (Templates & Registry):** لتوفير المكونات البصرية.
3. **نقل بيئة البناء (Build / BlueprintVideo):** لتفعيل نظام توليد الفيديو عبر البيانات (Data-Driven).
4. **نقل الـ API:** لتغليف النظام وجعله جاهزاً للتخاطب مع واجهة المستخدم المستقلة.
