import React from "react";
import { THEMES } from "../../remotion-app/src/remotion/scenes/v0";
import type { TemplateProps } from "../../../registry/types";

export const THEMESWrapper: React.FC<TemplateProps> = ({ surface, content }) => {
  return (
    <THEMES  />
  );
};
